apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
  name: {{ .Values.filedropui.name }}
spec:
  rules:
    - http:
        paths:
          - path: /
            backend:
              serviceName: {{ .Values.filedropui.name }}
              servicePort: http