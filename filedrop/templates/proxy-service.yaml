apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.filedropproxy.name }}
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app: {{ .Values.filedropproxy.name  }}